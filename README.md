# Center-out
This repository implements a unified bimanual–unimanual center-out reaching task in MATLAB. Both unimanual (L-only / R-only) and true bimanual trials share the same state machine, timing rules, black-screen synchronization, and NI-DAQ TTL logging.
